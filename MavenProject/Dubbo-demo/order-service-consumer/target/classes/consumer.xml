<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">
    <context:component-scan base-package="com.atck.gmall.service.impl"></context:component-scan>

    <dubbo:application name="order-service-consumer"></dubbo:application>

    <dubbo:registry address="zookeeper://zookeeper00:2181"></dubbo:registry>

    <!--配置本地存根-->



    <!--声明需要调用的远程服务的接口，生成远程服务代理-->

    <!--
        1.精确优先(方法级优先，接口级次之，全局配置再次之。)
        2.如果级别一样，则消费方优先，提供方次之。
    -->
    <!--timeout="0" 超时,默认是1000毫秒-->
    <!--retries="" 重试次数，不包含第一次调用，0代表不重试-->
    <!--幂等(设置重试次数)【查询、删除、修改】、非幂等(不能设置重试次数)【新增】-->
    <dubbo:reference interface="com.atck.gmall.service.UserService" id="userService"
    check="false" timeout="3000" retries="0" version="2.0.0" ><!--stub="com.atck.gmall.service.impl.UserServiceStub"-->
        <dubbo:method name="getUserAddressList" timeout="5000"></dubbo:method>
    </dubbo:reference>

    <!--配置当前消费者的统一规则,所有的服务都不检查-->
    <dubbo:consumer check="false" timeout="5000" version="*"></dubbo:consumer>

    <!--连接监控中心-->
    <dubbo:monitor protocol="registry"></dubbo:monitor>
    <!--<dubbo:monitor address="127.0.0.1:8080"></dubbo:monitor>-->
</beans>